# FastAPI Todo CRUD

A simple REST API for managing todo items, built as a demo submission for the Project Submission AI Analyzer.

## Features

- CRUD endpoints for todos
- Pydantic validation
- Modular route structure

## Run

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```
